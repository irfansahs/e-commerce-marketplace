# PostgreSQL
POSTGRES_USER=marketplace
POSTGRES_PASSWORD=change_me_local_only
POSTGRES_DB=marketplace
# Service databases (created on first init): identity_db, catalog_db, cart_order_db, inventory_db, payment_db, notification_db
# Host: localhost:5432  User: marketplace

# RabbitMQ
RABBITMQ_DEFAULT_USER=marketplace
RABBITMQ_DEFAULT_PASS=change_me_local_only

# Redis (no auth in local dev by default)
REDIS_HOST=redis
REDIS_PORT=6379
